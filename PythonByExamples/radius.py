#!/usr/bin/python3

import math

# Get radius of a circle
# Calculate area of circle: area = (pi * radius**2)

radius = float(input("enter radius: "))
pi = math.pi
areaOfCircle = pi * (radius**2)

print("Area: ", areaOfCircle)
